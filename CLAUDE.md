## Agent skills

### Issue tracker

GitHub Issues(`gh` CLI経由)。See `docs/agents/issue-tracker.md`.

### Triage labels

デフォルト5ラベルそのまま(`needs-triage`/`needs-info`/`ready-for-agent`/`ready-for-human`/`wontfix`)。See `docs/agents/triage-labels.md`.

### Domain docs

single-context(`CONTEXT.md` + `docs/adr/`)。See `docs/agents/domain.md`.
